spring:
  cloud:
    nacos:
      discovery:
        server-addr: 'localhost:8848'
      config:
        server-addr: 'localhost:8848'
        file-extension: yaml
    sentinel:
      transport:
        port: 8719
        dashboard: 'localhost:8080'
      datasource: # 将Sentinel放入nacos的数据库
        ds1:
          nacos:
            server-addr: 'localhost:8848'
            data-id: sentinel
            group-id: DEFAULT_GROUP
            data-type: json
            rule-type: flow

user:
  name: zmm
  age: 16