server:
  port: 8082
spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 'localhost:8848'
management:
  endpoints:
    web:
      exposure:
        include: '*'
